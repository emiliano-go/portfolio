# Katib GitHub activity API
# Get a GitHub PAT from https://github.com/settings/tokens (no special permissions needed)
# Used to avoid GitHub API rate limits when fetching activity data
KATIB_GITHUB_TOKEN=ghp_your_token_here
